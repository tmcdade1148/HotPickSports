export type {
  SeriesConfig,
  SeriesRoundConfig,
  TeamConfig,
  TabConfig,
} from '@shared/types/templates';

export type {
  DbSeriesMatchup,
  DbSeriesPick,
  DbSeriesScore,
} from '@shared/types/database';
