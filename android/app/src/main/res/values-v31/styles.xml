<resources>
    <!-- Android 12+: Skip BootSplash's Theme. Use our own splash_background
         drawable as windowBackground so the HotPick logo is visible from the
         moment the mandatory splash ends until React Native renders LoadingScreen.
         No black gap possible — the window background IS the splash. -->
    <style name="BootTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowSplashScreenBackground">@color/bootsplash_background</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>
