# HotPick — Launch Readiness Register (MASTER)

**The single home for everything that has to be true before launch — and everything that should be true eventually.** This is the version of record. It merges: Claude's scoring pass (repo), Code's live parallel audit (2026-06-25, verified against the live DB), the June 8 architecture audit, and the June 2 pre-launch checklist. If Code's repo copy (`docs/LAUNCH_READINESS.md`) and this drift, reconcile to this file.

Last updated 2026-08-26.

> **Reconciliation pass, 2026-08-26.** The register had drifted: six items marked
> "fix needed" or "pending apply" were already done, one had changed underneath
> its own entry, and roughly a month of work (everything after 2026-07-30) was
> missing. Every row touched below was re-checked against the live database or
> the repo on that date and is marked **(L2)**. **Rows NOT marked (L2) were not
> re-verified in this pass** and still carry their original June/July status —
> treat them as unchecked, not as confirmed.

### How to read this
- **Tier 0** = can corrupt a score or stall a live week. The only things that can truly sink the company.
- **Tier 1** = crashes, won't-open, or a user silently loses data. 1-star territory.
- **Tier 2** = slows you down or hides future bugs. Not user-facing.

**Status:** ✅ verified good · ⚠️ open risk · 🔧 fix needed (known) · 🔍 not fully checked
**Owner:** Tom · Code (live DB / repo / fixes) · Claude (synthesis / scoring logic / specs)
**Before launch?** Yes = before a user opens the app · Window = pre-launch prep, not launch week · No = after launch
**(L)** = verified against the LIVE database by Code on 2026-06-25.
**(L2)** = re-verified against the live database / repo on 2026-08-26.

---

## ✅ THE LAUNCH BLOCKER — RESOLVED 2026-06-25

| # | Item | Plain-language what & why | Status | Owner | Before launch? |
|---|---|---|---|---|---|
| **0.0** | **Picks are rejected on `nfl_2026` (status case mismatch)** | The `enforce_pick_lock` trigger checked `status != 'scheduled'` (lowercase), but the importer wrote all 318 `nfl_2026` games as `'SCHEDULED'` (uppercase) → every pick was rejected as "locked." **RESOLVED 2026-06-25:** trigger made case-insensitive (`lower(status) <> 'scheduled'`) via `apply_migration`. (L) Live-verified: fix present; uppercase SCHEDULED now allows picks; IN_PROGRESS/FINAL still lock; trigger `check_pick_lock` enabled. No data rows touched. **Full app-path confirmation (a real pick on UPPERCASE data through the app) is satisfied by the preseason go-live (0.11)** — `nfl_2026_pre` is ESPN-fed (uppercase, same as `nfl_2026`) and runs in REGULAR phase (pickable), so testers picking on it in August exercises the exact path. No need to flip `nfl_2026` (pre-season phase, pick flow not active) to test now. | ✅ **RESOLVED** (DB-verified) · app-path confirmed via preseason | Code (done) → preseason (0.11) | App-path via preseason (Aug) |

> **Fix note (revised 2026-06-25 after Code's full status-comparator scan):** My earlier call — normalize the 318 rows to lowercase + change the importer — was **wrong-direction.** Code traced every reader/writer: UPPERCASE is *canonical* for real (ESPN-fed) competitions (both ESPN writers produce it, the season-template client screens compare against it, all 318 rows hold it); lowercase is only the simulator's local convention. Normalizing to lowercase would have broken the exact-uppercase client screens and the sim filters. The correct, sufficient fix is **trigger-only**: make `enforce_pick_lock` case-insensitive (`lower(status) <> 'scheduled'`) so the one outlier matches every other reader — zero data churn, zero client blast radius. Manual backup first; apply via `apply_migration`; verify a scheduled-game pick saves and IN_PROGRESS/FINAL still rejects. Draft committed: `20260625120000_fix_enforce_pick_lock_status_case.sql`. **Cross-link: unblocks preseason (0.11) too.**

---

## TIER 0 — Can corrupt a score or stall a live week

| # | Item | Plain-language what & why | Status | Owner | Before launch? |
|---|---|---|---|---|---|
| 0.1 | Scorer idempotency | Re-running `calculate-scores` overwrites totals (`SET`, not `+=`) → cannot double points. (L) Re-verified. | ✅ verified | — | ✅ good |
| 0.2 | frozen_rank re-import clobber (T1-1) | Re-import omits rank columns → a locked rank can't be nulled. (L) Re-verified. | ✅ verified | — | ✅ good |
| 0.3 | Regular-season Ladder scoping | Sums from each pool's `pool_start_date`, joins on `user_id` only (moat intact). Mid-season pools start fresh. | ✅ verified | — | ✅ good |
| 0.4 | Ties / HotPick ±rank / zero-row backfill | Repo logic correct and test-locked. **But Code found live-scorer vs repo drift on tie handling (2.7)** — reconcile before a real tie occurs (rare in-season, not a blocker). | ✅ repo / ⚠️ live drift | Code | Reconcile (not a blocker) |
| 0.5 | `power_up` write-path hole | Scorer would double a HotPick on `power_up='double_down'`. (L) **Zero `power_up` rows exist; confirmed safe live.** Optional DB guard remains cheap hardening for when picks open at scale — not a blocker. | ✅ no exposure | Claude (optional spec) | Optional |
| 0.6 | Playoff double-count | (L) **CONFIRMED** in wk 19–22 data: the **Home rank chip** (`get_user_ranks_in_pools`) sums `week_points + playoff_points`, which are equal for wk ≥ 19 → doubles. **The Ladder itself is correct**, so the two screens disagree in the postseason. | ⚠️ confirmed | Code → Claude spec | Before playoffs (Jan) |
| 0.7 | Weekly engine / readiness gate | Odds-success is a hard gate before picks open (hardened June 11). No stall found in the audit. Confirm the manual recovery path + emergency brake (0.9). | 🔍 no blocker found | Code / Tom | Confirm |
| 0.8 | Anon-executable destructive RPCs (audit #14) | (L) **REMEDIATED.** `anon` revoked from every destructive function; all self-authorize by `super_admin`/pool-role; `admin_purge_user` dropped entirely. No Tier 0/1 security holes. | ✅ resolved | — | ✅ good |
| 0.9 | Emergency brake + runbook | `scoring_locked` pauses scoring with no deploy. Confirm it works AND you know the runbook cold. | 🔍 verify | Tom + Code | Yes |
| 0.10 | Sim-season dress rehearsal (regular season) | Run a full `nfl_2025_sim` **regular season** end-to-end, checking every score + Ladder position by hand. The scoring-confidence proof. (Playoffs/SB deferred per Tom.) | 🔍 to do | Tom (Claude designs checklist) | Yes |
| 0.12 | **Week-prep pipeline hardening — importer v29 + hourly Tuesday reconciliation** | **2026-08-25 incident:** the weekly `nfl-import-schedule` run 500'd (Akamai 403 — `site.api.espn.com` is hard-blocked from both Supabase networks; re-verified from the Edge runtime *and* `net.http_get` on 2026-08-25). It stamped `games_status='failed'` beside a **stale** `games_count=16`, blocking `admin_advance_week` + `open_week_picks` for preseason wk3 on a slate that was in fact complete. Opened only by manual override. Root cause is scheduling, not ESPN: the odds/rank/consensus jobs run every 4h and sailed through the same outage — the importer was the only weekly single shot. **Built on `fix/week-prep-hardening` (commit `115e33f`, not yet merged/deployed):** v29 attempt ladder (site.web.api ×2 → cdn ×2, spaced 5s), payload identity check, pick-conflict + slate-shrink delete guards, failure paths null `games_count` and re-arm `ready_notified_at`; jobs 58/60/61/96 → Tuesday-hourly; `run_week_prep_watchdog()` Tue 18:00+22:00 UTC. **TWO DATED PRECONDITIONS, both before Tue Sept 1 05:00 UTC:** (A) v29 deployed — the Sept 1 hourly chain is what preps week 1, and v28 403s; (B) `nfl_2026` advanced **PRE_SEASON → REGULAR** in the admin UI — `deriveWeek()` returns 0 during PRE_SEASON, so the whole chain no-ops until the phase flips. Miss either and week 1 does not self-prep. **Current state (live, 2026-08-25):** `nfl_2026` wk1 readiness is a fossil — games `pending`, odds 17/30 dated Aug 3, ranks `failed`, `rank` NULL on all 16 games; `open_week_picks` would fail today. Fallback if v29 slips: `hotpick-rank-check` skill → populate `rank`, then readiness override (`WEEK_READINESS_OVERRIDDEN`). **UPDATE — (L2) 2026-08-26.** Merged to main and the migrations are APPLIED: `run_week_prep_watchdog()` exists, job 58 is on `5 5-23 * * 2` (Tuesday-hourly), and `admin_audit_log` accepts `WEEK_READINESS_OVERRIDDEN`. **Precondition A is done. PRECONDITION B IS NOT: `nfl_2026` is still `current_phase = PRE_SEASON`, `week_state = idle`.** `deriveWeek()` returns 0 during PRE_SEASON, so the entire prep chain no-ops until the phase flips — the advance IS the switch that arms the pipeline. `week_readiness` for wk1 still reports `is_ready = false`, which is expected and self-corrects on the Sept 1 chain, but ONLY after the phase advance. No v29 invocation has been observed yet, which proves nothing either way: job 58 is Tuesday-only and the next fire is Sept 1. | 🔧 **A done · B OUTSTANDING** (L2) | **Tom — phase advance** | **Yes — before Tue Sept 1, 05:00 UTC** |
| 0.11 | **Preseason isolation — LOCKED (spec v1.1)** | DECISION (Tom, 2026-06-25): run Aug preseason as its **own competition** (`nfl_2026_pre`), fully scored for a real-world test; flip `active_competition` → `nfl_2026` at the Sept 2 cutover. Real season never touched. Tester accounts optional. **Verified this session (code trace, 2026-06-25):** **(a)** `nfl_2026` weeks 1–3 currently **conflate preseason + regular games under the same week integers** (30 / 32 / 31 games each) — preseason rows are `phase='PRESEASON'`, regular `'REGULAR'`, colliding only on the week number. **(b)** **No gameplay query filters by `phase`** — `nfl-rank-games` and `nfl-calculate-scores` scope by `(competition, week)` only; the *sole* phase-filtered query in the codebase is the home-screen recap. So a phase tag **cannot** isolate preseason — **the competition string is the only working isolation** (this rules out any "relabel the weeks in place" approach). **(c) HARD GATE:** `nfl-rank-games` assigns `rank = count − index` across *all* rows for the week, so a 30-game Week 1 would assign ranks 1–30 and freeze them — breaking the HotPick (caps at rank 16) and scoring. **Removing the `PRESEASON` rows from `nfl_2026` is a hard gate before `nfl_2026` opens for picks.** **(d) Importer gap:** `nfl-import-schedule` has no preseason branch (week ≤ 18 always pulls ESPN `seasontype=2`), so it can't populate `nfl_2026_pre` as-is — v1.1 adds a config-driven `seasontype` branch. Spec is now **v1.1** (`docs/PRESEASON_ISOLATION_SPEC.md`) — supersedes **v1.0** and the older `PRESEASON_PICKS_NO_SAVE_SPEC.md`; adds the `nfl_2026` PRESEASON-row cleanup + the importer branch. Depends on 0.0 (same importer). **RESOLVED — (L2) 2026-08-26.** Built and run for real: `nfl_2026_pre` played three preseason weeks, all 48 games FINAL + finalized for weeks 1–2, scored, with awards written. The hard gate is cleared — `nfl_2026` now holds **zero** `phase='PRESEASON'` rows. Remaining tail: the five `nfl_2026_pre` cron jobs (88/90/91/93/95) are **still active** and should be retired once week 3 settles (last kickoff 2026-08-29 22:00 UTC; 16 of 16 still unsettled as of this pass). Migration `260830_retire_nfl_2026_pre_cron_jobs.sql` is written and self-guarding — it refuses to apply until week 3 is FINAL, finalized, and has produced `season_user_totals` rows. | ✅ **RESOLVED** (L2) · cron teardown pending | Code (done) → Tom (apply 260830 after Aug 29) | ✅ done |

| 0.13 | **ESPN host block — `site.api.espn.com` is dead to us** | (L2) 2026-08-26. Akamai returns a hard 403 to that host from BOTH Supabase networks — the Edge runtime and the Postgres network — re-probed 2026-08-25 (403, 447-byte Access Denied page). It took the scoring pipeline down for 12+ hours on 2026-08-20/21 while pg_cron reported success the whole time, because `net.http_post` only QUEUES and never sees the response. `nfl-update-scores` moved to `cdn.espn.com` on 2026-08-21; `nfl-import-schedule` moved to `site.web.api.espn.com` (primary) + `cdn` (fallback) in v29. **Any code path that names `site.api.espn.com` again is a defect, not a fallback.** The one remaining reference is `espn-health-check`, which is inert (cron job 70, `active=false` since 2026-08-22). Truth about the pipeline lives in `net._http_response`, never in pg_cron's status. | ✅ resolved · host permanently retired (L2) | Code (done) | ✅ good |
| 0.14 | **Push delivery was unverifiable — `sent` was written unconditionally** | Until 2026-08-24, `process-notification-queue` marked every row `sent` after `fetch()` without checking `response.ok` and never fetched receipts. 178 rows read `sent` with zero errors, including 112 failed ESPN alerts to one user that looked like successes for 18 days. Now `pending → awaiting_receipt → sent\|failed`, and `sent` is written ONLY from an ok Expo receipt. **Known limit, measured not theorised:** an ok receipt still is not proof of arrival — on 2026-08-24 one token took three sends and three ok receipts with nothing arriving on any device. `sent` means "Expo reported delivery to the provider succeeded", which is a real improvement on "the POST returned 200" and is still weaker than "a human saw it". Do not let it become the next false signal. | ✅ resolved · meaning documented (L2) | Code (done) | ✅ good |
| 0.15 | **Reach: push covers 23%, Message Center 0%** | (L2) 2026-08-26. Only **34 of 148** users have an active push token, and the Message Center renders nothing for anybody (see the corrected 1.9). So the two in-app channels reach roughly a quarter and none of the user base respectively. A platform-wide broadcast today reports 148 recipients and can push to 34. This is the reason the welcome email (1.11) exists and the reason Chirps must keep carrying system messages. Not a bug in itself — a capacity fact that should be known before anyone relies on a channel to reach users during launch week. | ⚠️ known constraint (L2) | Tom (aware) | Know it |

---

## TIER 1 — Crashes, won't-open, or a user silently loses data

| # | Item | Plain-language what & why | Status | Owner | Before launch? |
|---|---|---|---|---|---|
| 1.1 | No ErrorBoundary anywhere | (L) Code: a single screen throwing an error **white-screens the entire app**. Add a top-level React ErrorBoundary so one bug can't kill the whole session. **RESOLVED — (L2) 2026-08-26.** `src/shell/components/AppErrorBoundary.tsx` exists and is wired at the top of `App.tsx`, wrapping the tree. Verified by reading both, not by the file's existence alone. | ✅ **RESOLVED** (L2) | Code (done) | ✅ good |
| 1.2 | Ladder not virtualized | (L) The Ladder renders **every** member with no `FlatList` virtualization → stall / out-of-memory on big pools. Switch to a virtualized list. **RESOLVED — (L2) 2026-08-26.** `SeasonBoardScreen` renders through a single `FlatList` with `scrollToIndex` and `onViewableItemsChanged`; the nested-ScrollView arrangement described here is gone. | ✅ **RESOLVED** (L2) | Code (done) | ✅ good |
| 1.3 | notification_preferences toggles don't persist | (L) Root cause: the single `ALL` RLS policy means a write succeeds only when `user_id = auth.uid()`, and the client upsert fired with a null/stale `userId`, so the write reverted ("reset" on relogin). **BUILT 2026-06-25 (drafted, not yet applied):** SECURITY DEFINER RPC `set_notification_preference(p_type, p_value)` — derives `auth.uid()` server-side (never trusts a client `user_id`), **whitelists `p_type`** against the 8 toggle columns with a **static CASE update (no dynamic SQL)**, pinned `search_path`, `EXECUTE` to `authenticated` only. Migration `supabase/migrations/20260625190000_set_notification_preference_rpc.sql` — **leave for Tom to review + backup + apply.** Client wired: `NotificationPreferencesScreen.togglePref` now calls the RPC and reverts on `error` (failure surfaces; no silent no-op). Client is JS-only (OTA-eligible) **but must ship only after the RPC is live.** **RESOLVED — (L2) 2026-08-26.** `set_notification_preference` exists in the live database, so the apply-order constraint is satisfied and the client is free to ship. | ✅ **RESOLVED** (L2) | Code → Tom (done) | ✅ good |
| 1.4 | Crash visibility (Sentry replacement) | Sentry **removed entirely** (native-startup crash + Xcode-26 iOS pod break). **BUILT:** `AppErrorBoundary` (register 1.1) + standalone `logError()` → `client_error_log` (no Sentry/DSN/native dep; throttled, no PII) + "surface-don't-swallow" rule (the #360 Ladder fix is the worked example). **Pending:** apply the `client_error_log` migration (after backup); Tom turns on Play Console "Android vitals" + Xcode → Organizer for native crashes. **PARTLY RESOLVED — (L2) 2026-08-26.** The `client_error_log` table is live, so the DB half is applied and `logError()` has somewhere to write. **The console half is still open and is Tom's alone** — Play Console "Android vitals" and Xcode → Organizer are the only route to NATIVE crashes, which `logError()` cannot see by construction. | 🔧 DB done (L2) · consoles open | Tom (consoles) | Yes |
| 1.5 | Email-confirm null-session onboarding | Fresh email signup can hit "Could not accept terms" when confirm-email is on. Interim fix in place (confirm off). Proper fix before public launch. **Correction 2026-07-30 (unverified):** the "silent missed global-pool enrollment" half of this item may not be real — enrollment appears to run off a signUp-time trigger chain (`auth.users` INSERT → `handle_new_user()` → `on_profile_created_enroll_global_pools`) that is session-independent, making the client RPC redundant. Read from trigger definitions only; not proven by a live signup. The `rpc_accept_tos` half of 1.5 is untouched and still real. | ⚠️ interim | Code + Tom | Decide |
| 1.6 | Device-matrix testing | Test on a real cheap/old Android, not just simulator. Layout + performance only show up here. | 🔍 to do | Tom only | Yes |
| 1.7 | compute-hardware deploy | Git is one commit ahead of the deployed function. Deploy so live == git. | 🔧 to do | Code + Tom | Yes |
| 1.8 | Live pick-splits don't refresh during games | `refresh-game-pick-stats` filtered `status.eq.live` (matches nothing — live games are IN_PROGRESS), so splits only refreshed after FINAL. **RESOLVED 2026-06-25:** filter → `%progress%` + `%final%` (case-insensitive), dead `status.eq.live` dropped (commit 502c9b0). **Deployed v15** (`verify_jwt=false` preserved). (L) Verified read-only: deterministic old-vs-new filter proof (`IN_PROGRESS`/`in_progress` now selected, FINAL still selected, scheduled excluded) + real-sim regression clean. Live mid-game `game_pick_stats` row folds into preseason (0.11) — no in_progress games exist now, and manufacturing one would irreversibly ratchet sim `week_state`. | ✅ **RESOLVED** (deployed + filter-verified) | Code (done) | ✅ done → preseason proves live |
| 1.9 | Global pool selection — `status` vs `is_archived` are decoupled | Original framing (2026-07-30) was wrong: it assumed `competition_config.global_pool_id` selects the enrollment target. It does not — nothing reads that key; it is documentation only. `auto_enroll_global_pools()` selects on `is_global = true AND status = 'active' AND deleted_at IS NULL`. `is_archived` is never consulted. Verified against the live function definition, 2026-07-30. Current state is CORRECT and nothing is broken: Platform Pool (`730d067e…`, `nfl_2026`, status active, hidden, 99 members) is the live target and is already the intended shape; legacy HotPick NFL 2026 (`0fb42ca5…`, status archived, 37 legacy members, not growing) fails the predicate and is correctly skipped — a silent no-op via `status`, not `is_archived`. **ACTION:** confirm Platform Pool is the intended target and leave `0fb42ca5…` archived. Do NOT "align" it to the preseason shape. **TRAP:** `archive_pool()` writes `is_archived` only and never `status`. Anyone tidying `status='active'` on `0fb42ca5…` to match an un-archived flag would put a second active global pool on `nfl_2026` into the loop, and every new user would join both. `0fb42ca5…` has both columns set, which neither function writer produces alone — `archive_pool()` writes only `is_archived`, `reset_reviewer_sim` writes only `status` and is scoped to reviewer-owned pools. Verified 2026-07-30: those are the only two function-layer writers of either column. Origin of this pool's both-set state is therefore direct SQL or a migration, and is not worth chasing — the pool is excluded by both conditions and is not growing. The existence of an unbounded manual-write path is the reason enforcement belongs at the reader (see 1.10), not at the writers. Also: the `nfl_2026_pre` `global_pool_id` description says "zero-member" but that pool has 47 — fix in the same pass. **THE SITUATION HAS INVERTED — (L2) 2026-08-26. Re-read this entry before acting on it.** The premise above ("Platform Pool `730d067e…` is status active, hidden, 99 members, and is the live enrollment target") is no longer true. `nfl_2026` now has **ZERO** pools matching `is_global AND status='active' AND deleted_at IS NULL`, so `auto_enroll_global_pools()` has no target and enrolls nobody. `730d067e…` (NFL26 Global) is archived with **0 active members**; so is `de2262f8…` (NFL26PRE Global). This is no longer "nothing is broken, confirm intent" — it is the direct cause of a live user-facing gap: **the Message Center reaches 0% of users.** `admin-broadcast` writes its inbox entry to whichever hidden global pool the lookup returns, and `HomeInbox` resolves platform pool ids from `get_my_pool_memberships()`, which filters `status='active'` — an empty set for everyone, so the banner renders nothing. Traced end to end 2026-08-25: the Aug 25 broadcast reached 20 people by push and nobody by Message Center. **The decision this needs is a product one** (which pool owns the Platform entry, and whether memberships get backfilled), not a bug fix — carried as an open decision in the Open Door spec §8. | ⚠️ **CHANGED — user-facing gap** (L2) | Tom (product decision) → Code | **Yes — Message Center is at 0% reach** |
| 1.10 | Global-pool enrollment doesn't exclude archived pools | Follows 1.9. Both enrollment functions gate on `status` alone, so a global pool archived through the normal organizer path stays `status='active'` and therefore stays in the auto-enroll loop. Latent, not live — it bites the first time a global pool is archived via `archive_pool()`. **Fix:** add `AND COALESCE(is_archived, false) = false` to the pool-selection predicate in both `auto_enroll_global_pools()` and `trigger_auto_enroll_global_pools()` — they carry identical predicates and the trigger is the live signup path; patching only the RPC leaves the real path broken. `COALESCE` not `= false`, so a null `is_archived` reads as "not archived" rather than silently dropping the pool from the loop. Enforced at the reader deliberately: there is more than one archive path (see 1.9), and the reader is the only place that covers all of them. No behavior change today (both live global pools have `is_archived=false`). [BACKEND], `apply_migration`, verification SELECT after. Ship before the preseason teardown. **Root cause, verified 2026-07-30:** two archive paths each write a different column — `archive_pool()` sets `is_archived` only; `reset_reviewer_sim` does `UPDATE pools SET status='archived'` and never sets `is_archived`. Neither writes both, which is why 9 pools sit `status='active'` + `is_archived=true`. This is why the predicate must require both conditions: either alone misses one archive path. Verified inert: every other function selecting `FROM pools` with an archive filter uses `is_archived = false` (`join_pool_by_code`, `join_pool_by_invite`, `join_public_contest`, `create_pool` tier count, `announce_regular_winners_on_phase`); the two enroll functions are the only readers of `pools.status`. The scoring path (`finalize_week_for_all_users`, `compute_pool_standings`) never selects `FROM pools` — user-scoped, unaffected. `is_archived` is the de facto canonical column; `pools.status` is vestigial outside the enroll pair. Post-launch: drop `pools.status` or make both writers set both. **STILL OPEN — (L2) 2026-08-26, confirmed unfixed:** neither `auto_enroll_global_pools()` nor `trigger_auto_enroll_global_pools()` mentions `is_archived` in its live definition. Note the interaction with the corrected 1.9: with zero active global pools on `nfl_2026`, this predicate currently selects nothing at all, so the bug is masked rather than gone — it re-appears the moment a global pool is made active again. Fix it in the same pass as whatever 1.9 decides. | 🔧 fix needed (L2) | Code → Tom (apply) | Yes — alongside 1.9 |

| 1.11 | **Welcome email — built, applied, SHIPS PAUSED** | (L2) 2026-08-26. First user-facing email HotPick has sent, and it stands up the reusable sending path (template in config, `email_log`, opt-out, cron). **Applied:** `email_log`, `notification_preferences.email_opt_out` + `email_unsub_token`, the three `welcome_email_*` config keys, and the `is_test_account` backfill (8 → **56** flagged, which is what stops 38 unflagged `@hotpicksports.com` accounts receiving a founder's welcome note). **Cron `send-welcome-emails` exists with `active = false`** and `email_log` holds 0 rows — shipped paused, exactly as specified. **Remaining, in order: deploy both Edge Functions → dry run → one live test to Tom → activate.** Safety: the `welcome_email_start_at` floor is set to 2026-08-25 22:26 UTC and NOTHING created before it is ever eligible; a missing floor key fails CLOSED (emails nobody). Blanking `welcome_email_body` is a second kill switch. **Open, and Tom's:** confirm `support@hotpicksports.com` is monitored — it is the reply-to and is named in the letter. | 🔧 applied · paused · awaiting deploy + dry run (L2) | Code (deploy) · Tom (activate + support inbox) | Before Sept 2 |
| 1.12 | **Deep links never reach JavaScript** | Carried in CLAUDE.md but never registered here. `expo-linking` is not installed and `AppDelegate.swift` overrides neither `open url` nor `continueUserActivity`, so nothing forwards a URL from `ExpoAppDelegate` into React Native's `Linking`. iOS opens the app for both `hotpick://` and `https://hotpick.app`, then the URL is dropped and `handleDeepLink` never fires. **Breaks tap-to-join invite links and password reset; manual code entry is unaffected.** Verified on device 2026-08-05 across custom scheme, universal link and email link. The fix is native and therefore **[RESUBMIT]** — deliberately held until after Week 1. Until it ships, invite codes go out as TEXT, never as links, which is why `buildInviteMessage` leads with the code. An app opening from a link proves only that iOS matched the URL, not that JS received it. | ⚠️ known · fix is [RESUBMIT], held | Tom (decide timing) | No — after Week 1 |

---

## TIER 2 — Tech debt / not user-facing

| # | Item | Plain-language what & why | Status | Owner | Before launch? |
|---|---|---|---|---|---|
| 2.1 | Migration baseline (audit #1) | Repo can't rebuild the live DB from scratch. Disaster-recovery insurance. Manual backup first. | ⚠️ open | Code | Window |
| 2.2 | Loose destructive script | `PURGE_PRESEASON_PICKS.sql` (a DELETE) was in `migrations/`. **DONE 2026-06-25:** `git mv`'d to `scripts/PURGE_PRESEASON_PICKS.sql` — out of the migration path so it can never be auto-applied by a replay/baseline pass. | ✅ done | Code | — |
| 2.3 | God files | (L) 11 large-file refactors; **3 are in-season hot-path — leave alone during a live week.** Decompose the safe ones (SmackTalk first) post-launch. | 🔍 later | Claude spec → Code | No |
| 2.4 | Dead simulator files | (L) **DONE 2026-06-25:** deleted `season-simulator.html`, `-v2.html`, `-v3.html`. **UPDATE 2026-06-30:** `season-simulator-v4.html` retired too (browsers now hard-block the `sb_secret_` key, so it could no longer write — `tools/sim-runner.mjs` is the protected-environment replacement, incl. the App-Review reviewer flow, see `docs/REVIEWER_SIM_RUNBOOK.md` §3). `hotpick-operator-console.html` renamed to `hotpick-operator-console_v2.html` (the single canonical console; CI spec-sync guard + SYNC comments repointed). Survivors: `sim-runner.mjs`, `hotpick-operator-console_v2.html`. | ✅ done | Code | No |
| 2.5 | June 8 architecture-audit leftovers (incl. doc accuracy) | Absorbs the remaining items from `ARCHITECTURE_AUDIT.md`: REFERENCE.md drift (§8 registry missing 6 fns, §9 directory tree, §3 config keys + OFF_SEASON lifecycle); verify mutable `search_path` is fully pinned; redeploy `nfl-weekly-transition` from source (stale entrypoint); `send-broadcast-email` is in repo but undeployed — decide enable-or-leave-off before launch; duplicate `sb_*`/`super_bowl_*` columns → dedupe during the Nov SB build. **None are launch blockers**; the audit's one scary item (#14 anon RPCs) is resolved at 0.8. `ARCHITECTURE_AUDIT.md` is **superseded by this register** — marked as such and moved to `docs/archive/ARCHITECTURE_AUDIT.md`. Lowest payoff-per-hour — do in the window / dead time. | 🔍 later | Code | No (window) |
| 2.6 | Public storage buckets | (L) Two enumerable public buckets (not in git). Confirm intentional; add path-prefix policy if any private content could land there. | 🔍 verify | Code | Window |
| 2.7 | Live scorer vs repo tie-handling drift | (L) The deployed scorer differs from the repo on how a tie is scored. Ties are rare in the regular season but possible. Reconcile so live == tested repo. | ⚠️ open | Code → Claude | Before a real tie |
| 2.8 | `reset_reviewer_sim` missing role check | (L) One sandbox-only RPC lacks a role check. Sandbox-scoped, low risk, but tighten. | 🔧 minor | Code | No |
| 2.9 | Status-casing consistency + dead code | Dual-cased system: real/ESPN = UPPERCASE (canonical), sims = lowercase. **The sandbox-render worry was a false alarm** — Code re-traced all six season-template comparisons (`SeasonMatchCard`, `SeasonPicksScreen`, `useSeasonSubmitState`, `seasonStore`); each `.toUpperCase()`s before matching, so lowercase sim data renders correctly (consistent with the reviewer sims that passed App Review). `enforce_pick_lock` was the only live exact-match outlier, fixed by 0.0. **DONE 2026-06-25:** dropped dead `prevent_write_after_kickoff` (verified 0 triggers / 0 referencing fns / 0 pg_depend dependents / 0 repo refs first; migration `20260625164358`). Optional long-term: route future status checks through the normalizer. No live risk. | ✅ confirmed safe · dead code dropped | Code | No |
| 2.10 | Cowork operator artifacts run with developer-level (RLS-bypassing) DB access | The Cowork operator artifacts (**Confirm & Open Picks**, **Confirm Ranks**, engine monitor) authenticate via the **Supabase MCP connector**. **No key is embedded** in the artifacts (good), **but** it executes SQL with **developer-level privileges — RLS-bypassing + DDL-capable** (confirmed this session: `apply_migration` ran DDL, `execute_sql` read RLS-protected rows) → **full SQL access to anyone with the owner's Cowork**. Supabase's own guidance is explicit — **"Don't connect the MCP server to production"** (it runs "under the context of your developer permissions") — which is the crux of (d). **(a) [Tom — not a repo task]** 2FA on the Anthropic (Cowork) + Supabase accounts. **(b) [Code — HELD; low priority; Confirm Ranks is a READ tool]** **Q2 resolved 2026-06-25:** the connector role is **NOT hardwired** — Supabase MCP supports a **`read_only=true`** URL param that "execute[s] all queries as a read-only Postgres user" (+ `project_ref` / `features` scoping). So a read-only Confirm Ranks is a **Cowork connector-config change (a second `?read_only=true` connector for the read tools), NOT a Postgres-role migration** — the role-migration idea is unnecessary; **do not draft it.** **(c) — FOLDED INTO (d):** confirmed no weekly-cycle *Edge Function* writes `admin_audit_log`, but retrofitting audit into each is the wrong fix; routing operator writes through the audited path makes the trail appear for the whole cycle at once. **(d) [Tom + Code — before launch] — gap named (Q1 + coverage):** the audited, `super_admin`-gated, `admin_audit_log`-writing **operator surface is exactly 3 RPCs** — `open_week_picks`, `admin_advance_week`, `admin_advance_season_phase` — already wired into the in-app `AdminSeasonControlScreen`. They cover **open / advance-week / advance-phase**; **lock is automatic** (`lock_at` + `enforce_pick_lock`). **Only gap: manual score + rank-freeze have NO audited operator RPC** (cron `nfl-calculate-scores`/`nfl-rank-games` only) — matters only for exceptional manual intervention. **No technical blocker** to routing routine prod writes through those RPCs / the in-app Console. **`sim-operator` is NOT a prod entry point** (hardcoded `SIM_ALLOWLIST`, refuses `nfl_2026` with 403, deliberately not config-driven — Q1). → (d) = **stop using Cowork raw SQL for prod writes; use the in-app Console / the 3 RPCs; add an audited RPC for manual score/freeze only if operators need it.** | (a) Tom 2FA · (b) HELD: config, not migration · (c) folded into (d) · (d) route prod writes via audited RPCs | Tom + Code | a: yes · b: low · d: **before launch** |
| 2.11 | Email confirmation → universal-link auto-login (post-launch polish) | **Post-launch polish (OTA, no resubmit).** Upgrade email confirmation from the interim/proper fix at **1.5** (which stops the null-session onboarding crash by holding the user on a "confirm your email, then sign in" state) to a seamless flow: Supabase redirects to **`https://hotpick.app/auth/confirm?...`** → the app opens on the universal link → the session is captured → the user lands in onboarding **already signed in**, with no manual re-sign-in step. **Infrastructure already exists** — `hotpick.app` universal links are wired for invite codes today (iOS **Associated Domains** entitlement `applinks:hotpick.app` + **Android App Links** `autoVerify="true"`), so no native change / store resubmit is required. **Needs (all JS/OTA):** (a) an **`/auth/confirm` branch in `handleDeepLink`** (`RootNavigator.tsx` — today it only handles `auth/reset` + invite `join`); (b) **`emailRedirectTo`** set on `signUp` (`EmailEntryScreen.tsx` — currently unset, so confirmation uses the Supabase Site URL); (c) **session capture** — either enable `detectSessionInUrl` or a manual `exchangeCodeForSession` on the confirm link (the client sets `detectSessionInUrl:false` today); (d) keep the **`/email-verified` web page as the not-installed fallback**. **Custom scheme (`hotpick://auth`) is NOT the path** — email clients block/strip custom-scheme links; the universal link is what opens reliably from an email. Cross-link: **1.5** (the proper fix this builds on). | 🔍 later (post-launch, OTA) | Code (build) + Tom | No (post-launch) |

---

## Running notes
- 2026-06-25: Register created (Claude scoring pass + June 8 audit + June 2 checklist).
- 2026-06-25: Preseason isolation LOCKED (own competition); 0.11 spec written for Code.
- 2026-06-25: **Code live audit merged.** New launch blocker 0.0 (pick status case mismatch). Confirmed: 0.6 playoff double-count (rank chip vs Ladder disagree), 1.1 no ErrorBoundary, 1.2 Ladder not virtualized, 1.3 toggle root cause. Resolved: 0.8 anon RPCs remediated; 0.1/0.2/0.5 confirmed safe live (0 power_up rows). New: 2.7 tie-handling drift, 2.6 public buckets, 2.8 sandbox RPC. Correction: keep simulator-v4 (live tool).
- 2026-06-25: Folded the June 8 `ARCHITECTURE_AUDIT.md` fully into this register (mostly 0.8 resolved + 2.1/2.2/2.5/2.6). That file is now superseded — retire it with a pointer here so there is only one register.
- 2026-06-25: Code's status-comparator scan **reversed the 0.0 fix direction** — uppercase is canonical; trigger-only case-insensitive fix is correct (my normalize-data plan was wrong, would have broken client screens). Added 1.8 (live splits stale during games) and 2.9 (status-casing class + dead `prevent_write_after_kickoff`). 0.0 migration drafted (not applied) on branch `claude/zen-tesla-upp18m`.
- 2026-06-25: **0.0 APPLIED + live-verified by Code — the season can accept picks.** App-path test on uppercase data folds into preseason (0.11); `nfl_2026` isn't pickable now (pre-season phase) so no contortion needed. Migration filename↔applied-version drift to reconcile (folds into 2.1).
- 2026-06-25: 1.8 live-splits fix drafted (502c9b0, not deployed). 2.9 sandbox-render concern **withdrawn** — Code confirmed the season-template screens normalize via `.toUpperCase()`; lowercase sim data renders correctly. (One of last turn's supporting arguments for the trigger fix — "lowercasing would break client screens" — was wrong; the decision still holds on its other reasons.)
- 2026-06-25: **0.0 migration filename reconciled** to the applied version (`20260625162854`); **dead `prevent_write_after_kickoff` dropped** (migration `20260625164358`) — closes 2.9's dead-code item.
- 2026-06-25: **1.8 deployed (v15) + verified.** `refresh-game-pick-stats` live-splits fix is live; `verify_jwt=false` preserved; filter proven (deterministic old-vs-new + real-sim regression). Live in-progress-game E2E folds into preseason (0.11).
- 2026-06-25: **2.4 done.** Deleted the three dead `season-simulator{,-v2,-v3}.html`; kept `-v4` (App Review) + Operator Console; trimmed deleted-file refs from `SERVICE_ROLE_KEY_ROTATION_RUNBOOK.md`.
- 2026-06-25: **Audit report archived** → `docs/audit/2026-06-25-launch-audit.md`; the register at `docs/LAUNCH_READINESS.md` is the single source.
- 2026-06-25: **✅ Merge gate RESOLVED.** Branch `claude/zen-tesla-upp18m` merged to `main` via **PR #353** (squash `ebb3ba2`, CI green). The 0.0 fix + both cleanup migrations (`20260625162854`, `20260625164358`), the register, and the dead-file cleanup are now **on `main`** — the repo↔live drift for *this session's* work is closed. **2.1 stays open** (the broader pre-May-2026 migration-baseline gap is unaffected). Branch re-synced onto merged `main` for continued work.
- 2026-06-25: **0.11 expanded** with the verified preseason-isolation findings (week 1–3 preseason/regular conflation; no phase-filtered gameplay query so competition-string is the only isolation; the `nfl-rank-games` 30-game `rank=count−index` HARD GATE; importer `seasontype` gap) and pointed at **spec v1.1** (supersedes v1.0). **2.2 done** (`PURGE_PRESEASON_PICKS.sql` → `scripts/`). **1.3** sharpened: RPC fix is ready/Code-owned (not yet built).
- 2026-06-25: **#354 merged → main** (squash `ecdfbd3`): the 0.11/v1.1 expansion, 2.2 (PURGE move), and 1.3 sharpening are now on `main`. Branch re-synced onto it.
- 2026-06-25: **Added 2.10** — Cowork operator artifacts authenticate via the Supabase MCP connector and run as `postgres` (service-role, RLS-bypassing); full unaudited SQL access to anyone with the owner's Cowork. Sub-items: (a) Tom 2FA, (b) Code → Confirm Ranks read-only, (c) **confirmed `nfl-open-picks` writes no `admin_audit_log`** (no audit trail on operator-driven prod picks-open), (d) Tom+Code decide Operator-Console/`sim-operator` vs Cowork for prod operator actions before launch.
- 2026-06-25: **Preseason spec committed** as `docs/PRESEASON_ISOLATION_SPEC.md` (v1.1); old `PRESEASON_PICKS_NO_SAVE_SPEC.md` banner-marked superseded; 0.11 reference repointed off the `.docx` to the in-repo markdown.
- 2026-06-25: **1.3 BUILT** (drafted, not applied). RPC `set_notification_preference` migration `20260625190000_*` — server-derived `auth.uid()`, whitelisted `p_type` + static CASE update (no dynamic SQL), pinned `search_path`, `authenticated`-only. `NotificationPreferencesScreen` wired to the RPC (reverts on error). **Apply order: RPC migration live (Tom: review+backup+apply) → then ship the JS-only client change via OTA/build.** Do not OTA the client before the RPC exists.
- 2026-06-25: **2.10(c) corrected** — the operator open-picks path (`open_week_picks` RPC) IS SECURITY DEFINER + writes `admin_audit_log` (as do `admin_advance_week`/`admin_advance_season_phase`); only the cron `nfl-open-picks` Edge Function doesn't log. The 2.10 risk is specifically **raw Cowork SQL bypassing** the audited + `super_admin`-gated RPC path, not a missing audit on the sanctioned path.
- 2026-06-25: **2.10 worked through (Q1, Q2, c→d, d-gap).** **Q1:** `sim-operator` is sandbox-only (hardcoded `SIM_ALLOWLIST`, 403 on `nfl_2026`, not config-driven) — not a prod entry point. **Q2:** Supabase MCP role is NOT hardwired — `read_only=true` runs as a read-only Postgres user, so (b) is a connector-config change, not a role migration → **held, not drafted.** **(d):** audited operator surface = 3 RPCs (`open_week_picks`/`admin_advance_week`/`admin_advance_season_phase`), already in-app; lock is automatic; only manual score/freeze lacks an audited RPC; no blocker to routing routine prod writes through them. **(c) folded into (d).** Supabase docs: "don't connect MCP to production."
- [2026-07-07] get_pool_pick_submissions — pre-lock HotPick leak CLOSED. Added a per-week time gate (now() < get_week_lock_time(p_competition, p_week) returns an empty set before the week's first kickoff), so no pool member can see another member's HotPick until the week locks. Also fixed a 42702 ambiguous-column bug in the membership guard (qualified the pool_members.user_id references). New helper get_week_lock_time(competition, week) returns MIN(kickoff_at) for the week. Both deployed via the Supabase SQL editor (not MCP — connector was hanging on writes). Demonstrated in nfl_2025_sim on 2026-07-07: as an active member, a locked week returned the seeded HotPicks; a future week returned empty; a non-member/null caller raised 42501. Test fixture seeded and fully reverted afterward — sim is clean.
- [2026-07-07] enforce_pick_lock — per-game lock replaced with whole-week lock. Picks now lock for the entire week once the week's first kickoff passes (now() >= get_week_lock_time(competition, week)), replacing the former per-game lock_at check. Status guard also dropped so the week lock is the sole gate. Game-not-found guard preserved. Demonstrated in nfl_2025_sim 2026-07-07: a pick change on a locked week is rejected (P0001 "Picks are locked for this week"); the same change succeeds when the week is unlocked. Sim reverted clean; nfl_2026 untouched.
- **Single-source handoff:** this file is the canonical register. Drop it into `docs/LAUNCH_READINESS.md` once; from here Code maintains the repo copy as items close. No parallel copy.
- Next: Claude builds the regular-season sim checklist (0.10).

[2026-07-10] get_player_week_picks — full-slate reveal RPC added. SECURITY DEFINER RPC returns a target member's full week slate (game_id, picked_team, is_hotpick), gated four ways: caller is an active pool member, target is an active member of the same pool, the week is locked (now() >= get_week_lock_time), and the pool is private. Private = is_public/is_global/is_designated_public all false AND owning_club_id IS NULL — ordinary private pools and partner-affiliated private pools included; public pools, global pools, and a partner's own official pool (owning_club_id set) excluded. Picks-only; client derives coloring. Demonstrated in nfl_2025_sim 2026-07-10: post-lock member call returns the slate; non-member raises 42501; a real member of an official partner pool (Mes Que) returns empty; sim reverted clean.

[2026-07-10] BUG RESOLVED (a4d5b3c): empty "Your Contests" after fresh login for sandbox testers. postAuthFlow.ts:83 calls fetchUserPools with the pre-resolution defaultEvent.competition (nfl_2026), even though fetchProfile (line 59) has already re-landed the tester onto nfl_2025_sim → pools fetched for the wrong competition → empty list until relaunch (LoadingScreen.tsx:137 already uses the resolved competition, which is why relaunch fixes it). Fixes: (1) postAuthFlow.ts:83 — read useGlobalStore.getState().activeSport?.competition (re-read getState, the captured `store` snapshot is stale), mirroring LoadingScreen.tsx:137; (2) fetchUserPools (globalStore.ts) discards the query error at BOTH line 243 (pool_members) and line 270 (ownedData) — capture and logError both. Repro: fresh login as a sim-only tester → empty Your Contests → relaunch → pools appear.

[2026-07-10] Lock-and-Reveal slice 1 — week-side ladder HotPick render verified on device; lock-time refresh trigger MISSING (gap). With Week 1 locked, Player 1's row correctly showed HotPick PHI (DAL @ PHI, rank 2, unscored); it renders purely off the time-gated RPC. BUT: the reveal does not appear on its own when a week locks — SeasonBoardScreen has no pull-to-refresh, no useFocusEffect, and re-fetches only on mount, pool-switch, and Realtime changes to season_user_totals / week_state. Locking by time (now() crossing first kickoff) touches none of those, so the reveal waits for an incidental refetch (device test required a manual app restart to update). Foundational gap for the whole feature — slices 2–5 also depend on a lock-time-aware refresh. Fix tracked as its own task before continuing slices. [→ RESOLVED 2026-07-10 by commit 108d1a9; see entry below.]

[2026-07-10] Lock-and-Reveal slice 1 — COMPLETE (render + refresh triggers). Week-side ladder shows a member's HotPick after the week locks, driven by the time-gated get_pool_pick_submissions RPC. Added two refresh triggers to SeasonBoardScreen so the reveal updates without an app restart: effect 5 (useFocusEffect → re-fetch on Board focus/foreground) and effect 4 (useCountdown on a Board-owned get_week_lock_time value → re-fetch when the week crosses lock). Effect 5 verified on device in nfl_2025_sim: no HotPick pre-lock → lock week → background/foreground → HotPick (Gunslinger, PHI, DAL @ PHI, rank 2, unscored) appeared with no restart. Effect 4 implemented and verified by inspection; live-timer path not isolated on device this session (sim time-state got tangled during testing). Board-owned lock-time fetch chosen over nflStore.weekFirstKickoff for phase-safety (weekFirstKickoff is null in preseason).

[2026-07-10] BUG FIXED: week-ladder name flicker ("Player N" clobber). fetchLeaderboard wholesale-replaced the shared userNames map (seasonStore.ts:710) using only scored users, clobbering week-only/unscored HotPick holders that fetchWeekLeaderboard had merged in — so a member's name flickered to the positional "Player N" fallback on the mount/pool-switch/realtime race. Would have hit most of the pool at week start (everyone unscored pre-scoring). Fix: fetchLeaderboard now merges ({...get().userNames, ...names}) instead of replacing, matching fetchWeekLeaderboard. Verified on device in nfl_2025_sim: name held stable ("The Gunslinger") across repeated tab switches, no placeholder flicker.

[2026-07-10] Lock-and-Reveal slice 3 — accordion slate colored by result (win/loss/pending). Each picked team in the full-slate accordion is colored by the server-computed season_picks.is_correct: true=win (green + ✓), false=loss (red + ✗), null=pending (neutral box, no mark); No-Pick rows stay unboxed muted text. HARD RULE held — client READS is_correct, never derives win/loss from scores/winner_team. [BACKEND] get_player_week_picks extended to return is_correct (pure column-add; the four gates — caller-member, target-member, private-pool, after-lock — unchanged), deployed via the Supabase editor; body version-controlled at supabase/migrations/260710_get_player_week_picks_is_correct.sql. [OTA] PlayerSlateAccordion colors the picked-team box + a subtle ✓/✗ mark (colorblind-safe redundancy; box color is never the sole signal). Verified on device in nfl_2025_sim 2026-07-10: with Week 1 locked and Player 1's is_correct hand-set to a mix, the accordion rendered 2 wins (green ✓), 1 loss (red ✗), 1 pending (neutral box), and 12 No-Pick — pending vs No-Pick confirmed visually distinct (boxed neutral pick vs unboxed "No Pick" text). SCOPE (honest): coloring verified against CONTROLLED is_correct values — the real column, hand-set — NOT through the live scorer. The finalize→score path isn't cleanly reversible in the sim (it ratchets week_state), so the scorer wasn't run; read+render path is proven, scorer correctness is separate and unchanged. Fixture (seeded picks + is_correct + week lock) reverted; sim clean.

[2026-07-13] Lock-and-Reveal slice 4 — persist reveal, then countdown to next lock (+ week-labeled clarity copy). After a week locks and reveals (slices 1–3), the week-side ladder persists that reveal through the next week's picks_open (displayedWeek = currentWeek-1); when the upcoming lock (weekLockAt = get_week_lock_time(currentWeek) = the real MIN(kickoff_at)) drops under an hour, a bold banner "WEEK {currentWeek} PICKS LOCK IN {unitText}" shows atop the reveal, and at the lock effect 4 re-fetches so the new week's reveal begins. Reuses slice 1's useCountdown tick: unit === 'minute' is the <60-min threshold (60:00 flip confirmed by the bucketing), gated on displayedWeek === currentWeek-1 so the displayed week and the count-down-to week stay adjacent; a null weekLockAt (post-Super-Bowl / no next week) reports isExpired → no banner. Gap-agnostic AND early-kickoff-correct by construction — reads the real MIN(kickoff_at), never a "+7 days" offset or a hardcoded time-of-day. Validated against nfl_2026 Week 12: the real Thanksgiving-Eve Wednesday-night (Netflix) game correctly makes MIN = Wednesday, so lock + countdown target Wednesday with zero special-casing. Clarity copy (all week numbers dynamic): Season tab "Points thru Week {lastFinalizedWeek}", Week tab "Week {displayedWeek} Points", banner as above. "PICKS" routes through LEXICON.picks (Hard Rule #9); "Week"/"Points" are generic (no lexicon entry). Device-verified in nfl_2025_sim 2026-07-13 (picks_open fixture — current_week=2, Week-2 first kickoff ~45 min out, 3 Week-1 rows seeded): banner "WEEK 2 PICKS LOCK IN 43 MINUTES" atop three ranked rows, Season tab "Points thru Week 1", Week tab "Week 1 Points". SCOPE (honest): banner + copy verified in picks_open. The Season-tab lastFinalizedWeek-vs-displayedWeek distinction — they diverge ONLY in the 'live' state, where lastFinalizedWeek = N-1 is the honest "last completed week" and displayedWeek = N would over-claim an in-progress week — holds by the state table, NOT separately device-tested in a live week. Fixture fully reverted; sim pristine (current_week=1, Week-2 lock 2026-09-18 00:15, 0 seeded totals). [OTA] display-only.

[2026-07-15] Added 2.11 — email confirmation → universal-link auto-login (post-launch polish, OTA/no-resubmit). Follows two read-only investigations this session: (1) deep-link handling — `hotpick://` scheme IS registered natively (iOS `CFBundleURLSchemes`, Android intent-filter) and `hotpick.app` universal links are fully wired for invite codes (iOS Associated Domains `applinks:hotpick.app` + Android App Links `autoVerify`), but `handleDeepLink` (RootNavigator.tsx) only branches on `auth/reset` + invite `join` — a bare `hotpick://auth` is a no-op, and `detectSessionInUrl:false` means no URL ever auto-establishes a session; (2) the "Could not accept terms" root cause (item 1.5) — with confirm-email on, `signUp` returns a user but a NULL session, and the app enters onboarding on `data.user`, stranding the user on the TOS gate with no JWT so `rpc_accept_tos` (and `auto_enroll_global_pools`) run as `anon` and are denied (both are `authenticated`-only; DB-verified). Client fix for 1.5: branch on `data.session` in EmailEntryScreen — only enter postAuthFlow with a real session; otherwise show a lexicon-sourced "confirm your email, then sign in" state (also fixes the silent missed global-pool enrollment). 2.11 is the *next* step beyond that fix — the universal-link flow that captures the session on confirm so the user lands already signed in. Custom scheme is explicitly NOT the path (email clients block it). Diff shown to Tom; not committed.

[2026-07-13] Lock-and-Reveal slice 5 — Picks screen reconciled to the whole-week lock (slice-1 enforce_pick_lock). The Picks screen locked per-game (SeasonMatchCard's lockAtPassed + picksAreOpen-during-'live') while the server locks the WHOLE week at first kickoff — so during 'live' a not-yet-started game showed editable but the server rejected the save (P0001). Fix: one shared helper utils/weekLock.ts (isWeekLocked = now >= MIN(kickoff_at) over the week's games; null-guarded so empty/all-null-kickoff read NOT locked, avoiding the now>=null→0 coercion trap) is the SINGLE lock source — SeasonMatchCard.isLocked (via a weekLocked prop), the Picks-screen section grouping, and the submit footer (useSeasonSubmitState) all read it, replacing three drifting per-game computations (one MIN, two call sites; the card computes nothing). Dropped the obsolete per-game lock_at term; submit footer gated on weekLocked (prevents a "Select your HotPick" prompt with every flame locked); savePick error softened to week-level ("Picks are locked for the week"). picksAreOpen unchanged (phase-meaning). Reads real MIN(kickoff_at), never per-game or hardcoded time — Thanksgiving-Eve Wednesday locks Wednesday; SB extra-week gap irrelevant. No 30s tick (relies on realtime status sub + focus refetch; add only if device shows a stale-editable window at the lock instant). Reviewer-impact: none in current nfl_2025_sim (picks_open, Sept-2026 games → weekLocked false → identical behavior); change only bites once a real week locks. DEVICE-VERIFIED nfl_2025_sim 2026-07-13, both directions: (1) normal case intact — cards editable + footer active in picks_open with future games; (2) bug window (whole-week MIN shifted just-past, statuses left scheduled, week_state left picks_open so weekLocked is the sole locker) — all cards locked including not-yet-started games, footer "PICKS ARE LOCKED" (no dead-end), whole-week alert copy, no "OPEN" header. Fixture reverted; sim pristine (wk1 MIN 2026-09-11 00:20, all scheduled). KNOWN GAP (punch-list, not this slice): Home screen PicksOpenHero doesn't reflect the whole-week lock — same root as the Home countdown discrepancy; reconcile in the Home-screen UI pass. [OTA] display + lock-gating only; server enforce_pick_lock already authoritative.

[2026-07-30] Added 1.9 (global pool state mismatch on `nfl_2026`), found during a config audit.

[2026-07-30] Corrected 1.9 — the original framing was wrong. Global-pool enrollment gates on `pools.status`, not `pools.is_archived`, and never reads `competition_config.global_pool_id`. The two columns are decoupled and `archive_pool()` writes only `is_archived`: treat "archived" as ambiguous and always check `status` when reasoning about global-pool membership. Also amended 1.5 with an unverified correction re: the trigger-based enrollment chain.

[2026-07-30] Added 1.10 — global-pool enrollment gates on `pools.status` while `archive_pool()` writes only `is_archived` and `reset_reviewer_sim` writes only `status`. Fix requires both conditions in both enroll functions. Verified: no other reader of `pools.status`; scoring path never selects `FROM pools`.

[2026-08-26] **RECONCILIATION PASS.** The register had gone four weeks without an update while August produced the densest run of launch-critical work in the project's history. Closed as already-done, each re-verified live rather than taken from a commit message: **0.11** (preseason isolation shipped, three scored weeks, zero PRESEASON rows left in `nfl_2026`), **1.1** (AppErrorBoundary wired in `App.tsx`), **1.2** (SeasonBoardScreen is a FlatList), **1.3** (`set_notification_preference` live). **1.4** moved to partly-resolved (table live; the consoles are still Tom's and are the only route to native crashes). **1.10** confirmed still unfixed. **1.9 inverted** — it now describes the opposite of the live state and is the cause of Message Center reaching 0%. Added **0.13** (ESPN host permanently retired), **0.14** (push `sent` was a lie for 18 days; what it means now), **0.15** (reach: 23% push, 0% Message Center), **1.11** (welcome email, applied and paused), **1.12** (deep links never reach JS — was only ever in CLAUDE.md).

[2026-08-26] **The single dated item that gates the season: `nfl_2026` is still `PRE_SEASON`/`idle` and must be advanced to REGULAR before Tue Sept 1, 05:00 UTC** (0.12, precondition B). `deriveWeek()` returns 0 during PRE_SEASON, so the hourly prep chain no-ops until the phase flips — the advance is the switch that arms the whole pipeline. Everything else on the Sept 1 path is deployed and applied.

[2026-08-26] Not risk items, so not given register rows, but they landed this week and change what launch week looks like: the **Open Door** (house Contest code on the Join screen, per-Contest Chirps posting switch enforced in RLS, member cap removed from the house Contest), the **Handoff** (the app now asks an organizer to invite somebody at creation — it previously never asked, anywhere, on any surface), the **broadcast** recipient + delivery-truth fixes, and the **compute-hardware** season-award guard (`manual_override` could crown a champion off partial standings, permanently, because awards are append-only with `ON CONFLICT DO NOTHING`).

> **Why this file went stale, and the one change that would stop it.** It is two documents wearing one hat: a register of open risk (the tier tables — should stay short and current) and a running log (the entries above — some are 800 words, and nobody re-reads them). Updating felt like writing an essay, so it stopped. Two suggested follow-ups, neither done here: **(1)** move the pre-August log to `docs/archive/` and keep a one-line-per-change changelog; **(2)** give the file a forcing function — a line in CLAUDE.md's Branch Rules saying a merge that opens or closes a register item updates this file in the same PR. CLAUDE.md is read every session; this file is not, which is exactly why it drifted.
